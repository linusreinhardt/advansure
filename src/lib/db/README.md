# lib/db — Datenbank-Zugriff (Schicht 3)

Persistenz über Supabase Postgres, geplant via Drizzle ORM.
Hier entstehen in den nächsten Schritten:

- `schema.ts` — Tabellen `claims`, `rooms`, `conversations`, `audit_logs`
- `client.ts` — Supabase-/Drizzle-Client (Server-seitig)
- `seed.ts` — Seed-Daten der Demo-Personas Leon, Robert, Julia

Bezug: PSP „Supabase-Backend aufsetzen", TU-01 / TU-06.
